# Development Log

## 2026-07-30

| Command or action | Result |
| --- | --- |
| `rg -n -i --glob MEMORY.md 'dynamic-hierarchy|hierarchy|research-protocol' ...` | No relevant prior-project memory entry found. |
| `Get-Item` and `Get-FileHash -Algorithm SHA256 -LiteralPath <protocol>` | Source existed; SHA256 verified as `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. |
| Checked `<project-root>` | Target was absent before creation. |
| `py -0p`, `python --version`, `python -m pip --version`, `winget --version` | Python 3.14.3 is the only launcher registration; `winget` is `v1.29.280`; Torch was not installed. |
| Initial `New-Item -LiteralPath` directory creation | Failed before mutation because `New-Item` does not implement `-LiteralPath`. Replaced with .NET exact-path directory creation. |
| .NET directory creation, `git init`, `Copy-Item -LiteralPath <protocol>` | Created the repository structure and copied the protocol without modifying the source. Destination SHA256 matches the source exactly. |
| Reviewed protocol and AMD official Windows documentation | Stage 0 is constrained to a normal baseline and synthetic tasks. AMD's current Windows matrix names Windows 11, Python 3.12, and RX 9060 XT; no ROCm install is attempted on Windows 10. |
| Read `docs/research-protocol.md` and wrote Stage 0 sources, configs, tests, and docs | The protocol constrains this stage to a normal baseline and hidden task truth. The terminal used a non-UTF-8 display code page, but the protocol file was not written after its SHA256 verification. |
| `winget install --id Python.Python.3.12 --exact --scope user ...` | Non-interactive, side-by-side Python 3.12.10 installation began. An early launcher check happened while `winget` was still running and correctly reported no runtime yet. |
| Waited for the existing `winget` process; `py -0p`; `py -3.12 --version` | Completed successfully: Python 3.12.10 is installed in the user profile; Python 3.14 remains the default launcher. |
| `python -m compileall -q src scripts tests` under Python 3.14 | Passed syntax compilation only. This is not a substitute for Python 3.12 runtime tests. |
| `git diff --check`; untracked-file inventory | `git diff --check` did not validate the new untracked files and must not be treated as whitespace evidence. A later full project-text check replaces this incomplete signal. |
| Created `.venv`; upgraded pip; installed the editable project and CPU-index Torch | Created only the project-local virtual environment, installed the editable package, and installed CPU PyTorch `2.13.0+cpu`. |
| Imported Torch from `.venv` | Torch `2.13.0+cpu` imported and GPU availability was `False`. Initial import warned that NumPy was absent, so NumPy is added before test execution. |
| Installed NumPy from the CPU wheel index; `pip check`; imported NumPy and Torch | Installed NumPy `2.4.4`; `pip check` reported no broken requirements; Torch remained `2.13.0+cpu` and GPU availability remained `False`. |
| `scripts/check_environment.py` in `.venv` | Confirmed Python `3.12.10`, Windows `10.0.19045`, Torch `2.13.0+cpu`, CPU execution, and registry qword values `[8539602944, 8539602944]`. |
| `.venv\\Scripts\\python.exe -m unittest discover -s tests -v` | Passed 5/5 tests: deterministic data, tensor shape/hidden truth, baseline forward shape, one training update, and candidate hierarchy interface behavior. |
| `.venv\\Scripts\\python.exe scripts\\train_stage0.py --config configs\\smoke.json --output-dir runs` | Completed CPU smoke training and wrote `runs/stage0-20260730T063358Z.json`. Metrics: final loss `4.163708686828613`, train accuracy `0.0234375`, evaluation accuracy `0.0625`. This deliberately tiny, single smoke run is execution evidence only, not a research result. |
| Final SHA256 check before warning cleanup | Protocol copy still matched `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. The prior `git diff --check` result is not a full-project whitespace validation because files were untracked. |
| Changed the baseline Transformer to post-normalization; repeated tests and smoke run | Tests again passed 5/5 without the original `norm_first` warning. Smoke still emitted PyTorch's optional nested-tensor prototype warning, wrote `runs/stage0-20260730T063450Z.json`, and reported final loss `4.194736003875732`, train accuracy `0.0078125`, evaluation accuracy `0.03125`. These low, stochastic smoke metrics are not interpreted as an experiment result. |
| Disabled the optional nested-tensor optimization; prior focused test, smoke, compile, dependency, and hash checks | Passed 5/5 tests in `1.147s`; smoke wrote `runs/stage0-20260730T063527Z.json` without warnings and reported final loss `4.194736003875732`, train accuracy `0.0078125`, evaluation accuracy `0.03125`; `compileall` and `pip check` passed. The protocol SHA256 still matched the supplied source exactly. `git diff --check` was not proof for untracked project files. |
| Independent review remediation | Confirmed two P1 defects: variable bindings allowed duplicate variable IDs, and repeated-symbol queries had a fixed prefix. Replaced both generators, added PAD-aware repeat batches, length-scale evaluation, strict CPU controls, provenance, lock file, config validation, and focused tests. |
| Reviewed AMD current Radeon limitations | AMD documents Windows as an environment entry point with Python 3.12, but separately lists no ML training support. The project remains CPU-only; any future AMD GPU training must recheck and prefer the official Linux route. |
| Queried `.venv` dependency set | Recorded the exact CPU lock set: Torch `2.13.0+cpu`, NumPy `2.4.4`, and all current Torch transitive dependencies. |
| Expanded unit test, compile, and project-text checks | Passed 8/8 tests in `1.217s`; Python 3.12 `compileall` passed; `scripts/check_project_text.py` inspected 19 project text files and found no trailing whitespace. |
| Final CPU smoke and environment validation | `runs/stage0-20260730T064756Z.json` completed with strict deterministic algorithms and one Torch CPU thread. It records schema `2`, Python `3.12.10`, Torch `2.13.0+cpu`, NumPy `2.4.4`, dependency versions, and source-manifest SHA256 `2b70e3ce54eb30c46ea3fe182efae786e09c46630b776ae2f972dadfb1530dc1`. Per-task 1x/2x/4x results were written explicitly. Final loss was `3.8232181072235107`; results remain smoke evidence only. |
| Final source-state tests and run-record audit | Passed 8/8 tests in `1.087s`. Audited the run JSON fields for schema, strict determinism, fixed thread count, runtime versions, source manifest, and separate 4x accuracies. |
| Final remediation verification | Passed 8/8 tests in `1.243s`; CPU smoke wrote `runs/stage0-20260730T064912Z.json`; `compileall` and `pip check` passed; the complete project-text check covered 19 files with no trailing whitespace; protocol SHA256 remained `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. The deterministic smoke repeated the same per-task/per-scale metrics as the preceding run and is still not a research result. |
| Provenance and repeat-prefix follow-up review | Confirmed that recursive `src/` manifest scanning could include `__pycache__` and editable-install `.egg-info`, and that repeat prefixes could be empty. Replaced the manifest with an explicit authored-file whitelist and restricted repeat prefixes to `1..body_length-1`. |
| Added provenance isolation and minimum-length tests | The test suite now creates `.pyc`, `.egg-info`, `.venv`, `runs`, and `data` artifacts in a temporary project and proves that the manifest is unchanged. Configuration now rejects `repeat_length < 3`, ensuring nonempty prefix positions can vary in a batch. |
| Follow-up test and syntax/text validation | Passed 9/9 tests in `1.314s`; Python 3.12 `compileall` passed; project-text check inspected 19 files with no trailing whitespace. |
| Final provenance smoke and audit | `runs/stage0-20260730T065235Z.json` completed; `pip check`, text check, and protocol SHA256 validation passed. Its source-manifest SHA256 is `95b86001b610297e4a27d6b849ed441928cb6e35b368cea3d9a39ca4b4885220` and lists exactly 14 allowed authored files with zero cache, `.pyc`, `.egg-info`, `.venv`, `runs`, or `data` entries. An initial read-only audit command had a PowerShell quoting error; its corrected rerun passed without file mutation. |
| Final P2 train-contract and Python-support remediation | Added an end-to-end `train()` test with two training steps, batch size two, one evaluation batch, and scales `1/2/4`; it asserts both task keys, all scale keys, and exact totals. Tightened package support to Python `>=3.12,<3.13`, matching the only verified runtime. |
| Final P2 verification | Passed 10/10 tests in `1.294s`; smoke wrote `runs/stage0-20260730T070134Z.json`; `compileall`, `pip check`, and the 19-file project-text check passed; protocol SHA256 remained unchanged. The new run manifest SHA256 is `be77bb8653a3b478350d8795708b5d2820e0e3d1a2a96034d7803ed991a1ff65`, contains 14 allowed files, and records both train tasks plus scale keys `1`, `2`, and `4`. |
| Created isolated DirectML environment | Created `.venv-directml` with Python 3.12.10 without modifying `.venv`. The first pip child process stalled after package metadata with no network or file progress and was terminated by exact PID; a clean retry completed. |
| Installed and locked DirectML dependencies | Explicit pre-release install resolved `torch-directml 0.2.5.dev240914`, `torch 2.4.1`, `torchvision 0.19.1`, and `numpy 2.5.1`. Added `requirements-directml.lock`; `requirements-cpu.lock` remains unchanged at Torch `2.13.0+cpu` / NumPy `2.4.4`. |
| Minimal DirectML device validation | On `privateuseone:0`, device name was `AMD Radeon RX 9060 XT`; import, tensor addition, matrix multiplication, and autograd/backward passed. Synchronization used `.cpu()`/`.item()`. |
| Implemented backend and metrics contract | Added CPU/DirectML resolution, device name and determinism metadata, parameter count, synchronized training time, steps/s, examples/s, nonzero-gradient backward evidence, captured runtime warnings, and backend-specific run filenames. Candidate hierarchy remains disconnected. |
| Avoided two observed DirectML-incompatible operator paths | Default AdamW emitted an unsupported `lerp` warning and eval emitted an unsupported fused `_transformer_encoder_layer_fwd` warning. Replaced the former with the same dense AdamW core equations on both backends and disabled the fused eval fastpath on both. Later warning lists are empty, but this is not evidence of zero silent CPU fallback because DirectML exposes no public fallback counter here. |
| Added DirectML and optimizer tests | Added a DirectML-only device/backward test with CPU skip, CPU/DirectML config validation, performance metric assertions, and a one-step equivalence test against PyTorch AdamW. |
| Final dual-environment tests | CPU environment passed 11 active tests with one DirectML skip in `1.236s`; DirectML environment passed all 12 tests in `0.867s`. Both environments passed `pip check` and Python compilation. |
| Final CPU smoke | `runs/stage0-cpu-smoke-20260730T082456Z.json`: 45,120 parameters, `0.1037961s`, `154.1484 steps/s`, `2466.3740 examples/s`, backward passed, zero runtime warnings. |
| Final DirectML smoke | `runs/stage0-directml-smoke-directml-20260730T082503Z.json`: RX 9060 XT, 45,120 parameters, `0.3305099s`, `48.4100 steps/s`, `774.5608 examples/s`, backward passed, zero runtime warnings. |
| Final CPU benchmark | `runs/stage0-cpu-benchmark-cpu-20260730T082511Z.json`: 4,830,000 parameters, 10 steps, `0.3962090s`, `25.2392 steps/s`, `201.9136 examples/s`, backward passed, zero runtime warnings. |
| Final DirectML benchmark | `runs/stage0-directml-benchmark-directml-20260730T082518Z.json`: RX 9060 XT, 4,830,000 parameters, 10 steps, `0.5194590s`, `19.2508 steps/s`, `154.0064 examples/s`, backward passed, zero runtime warnings. DirectML was functional but slower than CPU on this workload. |
| Final run provenance audit | All four final runs record Python 3.12.10, their actual Torch/NumPy versions, backend/device, synchronization and determinism status, and the same 22-file source manifest SHA256 `7207ad5ce7030c41858255f51ed7594452fd6339e58b4650048acf54be68ab00`. |
| Terminal dual-backend acceptance | CPU environment passed 11 active tests with one DirectML skip in `1.371s`; DirectML environment passed 12/12 in `0.936s`; both `pip check` commands passed; 27 project text files had no trailing whitespace; benchmark configs were structurally identical except for `device`; protocol SHA256 remained `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. No Git commit was created. |
| Independent benchmark review remediation | Added explicit `fallback_observability` with DirectML status `unknown`; zero Python warnings are never interpreted as zero fallback. Moved the final timing barrier to an updated `classifier.weight` after the last optimizer write and before stopping the timer; loss/gradient reads now occur outside the timed region. |
| AdamW core subset validation | Renamed the optimizer to `DirectMLCompatibleAdamWCore` and documented its intentionally limited dense AdamW surface. Added closure execution under `torch.enable_grad()`, a 25-step non-default hyperparameter/state comparison against `torch.optim.AdamW(foreach=False)`, and a DirectML 25-step state-device test. |
| Determinism and warmup contract | DirectML now also applies `torch.set_num_threads(cpu_threads)`. CPU smoke remains strict deterministic. Performance configs explicitly disable deterministic algorithms on both backends; DirectML fails closed if `deterministic=true`. Warmup performs real updates and is excluded from timing. |
| Repeated benchmark runner | Added `scripts/benchmark_stage0.py`, requiring at least three repeats and retaining every sample plus median, min, max, and nearest-rank approximate p95. All benchmark timing uses an updated-parameter barrier before and after the measured region. |
| Minimal DirectML revalidation | `scripts/check_directml.py` passed import, device selection, addition, matrix multiplication, and autograd/backward on `privateuseone:0` / AMD Radeon RX 9060 XT. It observed no Python warnings and records fallback status as unknown because no public DirectML fallback counter is available. |
| Revised CPU smoke | `runs/stage0-cpu-smoke-20260730T084413Z.json`: strict deterministic algorithms enabled, 45,120 parameters, 16 measured steps, `0.0988824s`, `161.8084 steps/s`, backward passed, no Python warnings. Source manifest SHA256 `2e468f126813ef142ed34b17d70ef1e8a72cd92eee9f25cca3235e38b8ca779a`. |
| Revised DirectML smoke | `runs/stage0-directml-smoke-directml-20260730T084421Z.json`: deterministic algorithms disabled as required, RX 9060 XT, 45,120 parameters, 16 measured steps, `0.3322939s`, `48.1501 steps/s`, backward passed, no Python warnings, fallback observability unknown. Manifest matches the CPU smoke. |
| sustained-short repeated benchmark | CPU file `runs/benchmark-cpu-benchmark-sustained-short-cpu-20260730T084444Z.json`: 4,830,000 parameters, batch 8, 10 warmup plus 100 measured steps, median `28.1403 steps/s` / `225.1224 examples/s`, range `27.7310-28.3176 steps/s`. DirectML file `runs/benchmark-directml-benchmark-sustained-short-directml-20260730T084506Z.json`: median `22.0489 steps/s` / `176.3914 examples/s`, range `20.4467-22.7394 steps/s`; DirectML/CPU median ratio `0.7835x`. |
| throughput-long repeated benchmark | CPU file `runs/benchmark-cpu-benchmark-throughput-long-cpu-20260730T084532Z.json`: 4,871,040 parameters, batch 16, vocabulary 128, repeat length 32, binding pairs 16, 5 warmup plus 40 measured steps, median `7.0472 steps/s` / `112.7547 examples/s`, range `6.9645-7.5400 steps/s`. DirectML file `runs/benchmark-directml-benchmark-throughput-long-directml-20260730T084551Z.json`: median `17.4782 steps/s` / `279.6512 examples/s`, range `16.6820-18.5470 steps/s`; DirectML/CPU ratio `2.4802x`. Evaluation scale is intentionally only 1 because this is a throughput load, not length-extrapolation evidence. No batch reduction was needed. |
| Benchmark interpretation correction | The old 10-step CPU/DirectML benchmark is retained only as an initial short run and is not the final comparison. Paired configs and optimizer algorithm match except for device, but the environments differ: CPU uses Torch `2.13.0+cpu` / NumPy `2.4.4`; the DirectML runtime reports Torch `2.4.1+cpu` / NumPy `2.5.1` with `torch-directml 0.2.5.dev240914`. Accuracy is not used as a performance or research conclusion. |
| Final review verification | CPU tests passed 16 with 3 expected DirectML skips in `1.388s`; DirectML passed all 16 in `1.093s`. Both Python 3.12 environments passed `pip check`; both passed `compileall`. The project-text checker inspected 33 files with no trailing whitespace. Source and copied protocol SHA256 both remained `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. All six revised smoke/benchmark JSON files record source manifest `2e468f126813ef142ed34b17d70ef1e8a72cd92eee9f25cca3235e38b8ca779a`. An initial version-audit expression tried the absent `torch_directml.__version__` attribute after a successful `pip check`; the corrected `importlib.metadata.version` command exited successfully and reported `0.2.5.dev240914`. No Git commit, OS update, or driver change was made. |
| Stage 1 protocol audit | Re-read the protocol's Stage 1 and stopping rule. Implemented only ordinary Transformer A and true-structure diagnostic D. Learned `MERGE/STOP` remains unstarted Stage 2 work and no adaptive hierarchy claim is made. |
| Nested-expression task | Added randomly ordered unique variable bindings, randomly oriented nested modular add/subtract trees, repeated variable leaves, automatic labels, hidden postorder trees, source references, and depth scales 1/2/4. Tests independently recompute every label and confirm tree nodes contain neither labels nor intermediate values. |
| True-structure D | Added one shared recursive composition module over contextual leaf and operator source states. It consumes generator tree references only, retains original leaf references, and records recursive depth and combined-node count. Pilot A has 79,616 parameters and D has 79,232; the 384-parameter difference is recorded rather than called equal. |
| Cooperative runtime | Added paired A/D microbatches, microbatch 4, gradient accumulation 4, effective batch 16, two CPU threads, 100 ms yield after each optimizer update, BelowNormal priority request, 20-second atomic heartbeat, PAUSE/RESUME/STOP controls, and CPU/RAM hysteresis. DirectML has no hard VRAM quota in this runner. |
| Resource guard validation | Unit test confirmed pause after three consecutive pressure samples and resume only after two consecutive recovery samples with CPU 85/75% and RAM 6/8GB thresholds. GPU utilization is not treated as conflict pressure. |
| Snapshot and checkpoint implementation | Added unique frozen snapshots containing authored source, JSON configs, Python/PowerShell scripts, tests, both locks, README/AGENTS, Stage 1 design, and protocol copy. Excludes virtual environments, caches, generated data, and old runs. Atomic checkpoints include A/D models and optimizers, global step, metrics, Torch CPU RNG, generator RNG, config, elapsed time, source manifest, and snapshot hash. |
| DirectML D crash diagnosis | The first per-sample, per-node D implementation caused native Windows heap corruption `0xC0000374`; A at 4x remained functional. Batched each recursive level across samples while preserving every sample's actual left/right/operator references. DirectML D forward, backward, checkpoint, and resume then passed. |
| Stage 1 focused tests | CPU environment passed 23 tests in `1.569s` with 4 expected DirectML skips. DirectML passed all 23 in `1.568s`, including A/D full paired updates, gradient accumulation, optimizer-state device residency, checkpoint save/load, and continued training after resume. |
| Frozen CPU smoke | `runs/stage1-smoke-cpu-20260730T094337Z` completed 2 paired steps with bootstrap and step 1/2 checkpoints. A/D forward, backward, scale 1/2/4 evaluation, BelowNormal priority, resource sample, and atomic result/status all completed. Tiny accuracies are not interpreted. |
| Frozen DirectML smoke | `runs/stage1-smoke-directml-20260730T094359Z` completed 2 paired steps with bootstrap and step 1/2 checkpoints. Final A/D losses were `4.824718` and `4.216122`; fallback observability is `unknown: no public DirectML fallback counter; no Python warnings observed`. |
| Hidden launcher smoke | `runs/stage1-20260730T094420Z` launched with `Start-Process -WindowStyle Hidden`, wrote separate empty stdout/stderr logs and `pid.json`, completed step 2, and produced its scheduled checkpoint. No residual process remained. |
| Resume remediation and retest | First resume attempt exposed two launcher defects: PID replacement used an invalid null backup path, and resume defaulted to the pilot config rather than the original smoke config. The mistakenly launched worker failed closed on shape mismatch; no source/run was deleted. Fixed atomic PID backup, original snapshot-config selection, config equality validation, and emergency-checkpoint protection. Restored the prior valid latest pointer and successfully resumed from `checkpoint-00000002.pt` to completed step 2. |
| Formal background pilot launch | Started `runs/stage1-20260730T094624Z` at `2026-07-30T09:46:26.7335475Z` from frozen snapshot hash `d8c0639e5dc054f34a10f0fbe7ba532d2704d00ac69ab37fdd27b8ed9cabc103`, DirectML RX 9060 XT, default 120-minute budget. `pid.json` records live virtual-environment launcher PID `26964`; heartbeat records worker interpreter PID `12792`, its child. Bootstrap checkpoint was created before updates. |
| Formal pilot observation | After approximately 81 seconds, both launcher and worker remained alive and state `running` at paired step `224`; latest checkpoint was `checkpoint-00000200.pt`. Latest A/D losses were `2.054488` / `2.018433`. Host CPU was `39.2%`, available RAM `19.38GB`, priority applied, no pause/OOM/Python warning, stdout/stderr empty, and fallback observability unknown. A later lightweight check found step `501` and checkpoint `500`, confirming continued progress. The early scale accuracies were recorded in heartbeat but are not a mechanism conclusion. |
| Stage 1 prelaunch verification | Both environments passed `pip check`, `psutil 7.2.2` import, and compileall. Project-text validation covered 46 files with no trailing whitespace. Protocol source and copy both remained SHA256 `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. No Git commit, OS update, driver update, environment removal, or run cleanup occurred. |
| Final handoff heartbeat | At `2026-07-30T09:50:31.623948Z`, launcher PID `26964` and worker PID `12792` were both alive, state `running`, paired step `667/100000`, elapsed `242.297s`, A/D losses `2.104236` / `2.098301`, and checkpoint `checkpoint-00000600.pt`. Host CPU was `36.0%`, available RAM `17.97GB`, no pause or warnings were present, and fallback observability remained unknown. |
| PID role audit | Confirmed the Windows venv redirector process tree: launcher `26964` owns child base-Python worker `12792`; heartbeat is authored by `12792`. At `383.875s`, the worker remained running at step `1036` with checkpoint `1000`, CPU `58.0%`, RAM `17.07GB`, no pause/warnings, satisfying the requested observation beyond 120 seconds. |
| Future PID registration repair | Added launch-ID worker registration. New `pid.json` files preserve `launcher_pid`, atomically register `worker_pid`, switch compatibility field `pid` to the worker, and record PID role/state. Duplicate checks prioritize heartbeat PID and `worker_pid`, then validate the live process command line contains both `stage1_worker.py` and the exact run directory; launcher PID is only a 60-second startup guard. |
| Current-run metadata and safety | Updated only the live run's `pid.json` metadata to identify launcher `26964` and worker `12792`; the frozen snapshot and processes were not modified. A real resume attempt against the still-running run was correctly rejected as `live worker: PID 12792`. Control files remain worker-polled and checkpoint/resume remains safe. |
| Legacy snapshot recovery compatibility | The current run's frozen worker predates `--launch-id`. The active launcher now detects worker support before passing that argument. Current/older snapshots resume without it while retaining heartbeat-based duplicate protection; future snapshots use the full handshake. |
| PID repair validation | CPU suite passed 24 tests with 4 expected DirectML skips, including atomic registration preserving launcher identity. Python compileall and PowerShell parser checks passed. No second DirectML worker was started. During validation the formal worker advanced to step `1340`, checkpoint `1300`, CPU `61.4%`, RAM `18.97GB`, with no pause or warning. |
| Structure-only D boundary | Replaced D's complete-truth argument with frozen `StructureOnlyBatch`, `StructureSample`, `LeafSourceReference`, and `MergeSourceReference` types. They expose topology and source indices only. Adversarial tests verify value/label/intermediate fields are absent and complete truth raises a runtime type error. Legitimate serialized binding tokens remain visible equally to A and D. |
| Recursive-step provenance | D now increments recursive-step telemetry only after an actual batched shared `compose` call. A counting wrapper test verifies reported steps equal module invocations and are unaffected by poisoned full-truth recursive metadata. |
| Topology scope correction | Documented that the current generator is a randomly oriented skew chain: each internal node has one deep child and one leaf. Current scale 1/2/4 results are chain-depth/length extrapolation only. Balanced/branched generation and held-out topology are required next work before general topology claims. |
| Recovery semantics correction | Added `recovery_state` to heartbeat/result and checkpoint metadata. Recovery is periodic at-least-once; a crash may replay up to `checkpoint_steps` paired updates. Tests verify the uncheckpointed window grows after updates, resets after atomic save, and is zero immediately after load. No exact-once claim remains. |
| Per-run TOCTOU locks | Added exclusive launcher `.launch.lock` across check/start/PID publication and an actual-worker Windows named mutex keyed by resolved run path. Tests verify concurrent launcher file acquisition is rejected, a second worker thread cannot acquire the named mutex, and acquisition succeeds after release. Current frozen run is unchanged. |
| Scientific-boundary validation | CPU passed 27 tests with 4 expected DirectML skips in `1.482s`; DirectML passed all 27 in `1.620s`. Both include structure leakage, compose count, recovery window, and named mutex tests. The first launcher-lock probe had a PowerShell `catch` spacing syntax error and made no mutation; the corrected probe passed. During validation the formal frozen worker remained healthy and advanced beyond step `3081`/checkpoint `3000`. |
| Formal Stage 1 completion | `runs/stage1-20260730T094624Z` reached its 120-minute limit normally at paired step `20160`. Each model saw `322560` examples; the final atomic checkpoint is `checkpoint-00020160.pt`. The worker exited after writing `completed` / `time_budget_reached`; stdout/stderr remained empty and no runtime warning was recorded. |
| DirectML activity audit | Windows per-process GPU counters observed worker PID `12792` on RX 9060 XT `Compute 0` at approximately 20-25% across five samples, with `153075712` bytes dedicated and `47542272` bytes shared GPU memory. This confirms observed GPU work but does not change fallback observability from `unknown`. |
| Large final-checkpoint evaluation | The built-in 16-example-per-scale result was rejected as too small for interpretation. A read-only CPU evaluation of `4096` fixed examples per scale found A and D made identical constant predictions on all `12288` examples. Accuracy was `17.3096%`, `11.5234%`, and `12.1582%` at scales 1/2/4; no D-over-A or length-extrapolation signal was observed. |
| Fixed-batch learnability diagnostic | Fresh CPU A and D models using the same optimizer core could memorize one fixed 64-example batch. D reached a checked 100% fit after 25 updates and A after 125. This verifies non-inert gradients and update paths only; it does not establish rule learning. |
| Stage 1 stopping decision | The pilot result is negative for this configuration. Learned `MERGE/STOP` remains unstarted. The next gate is a revised Stage 1 curriculum, output-distribution controls, multi-seed large evaluation, and balanced/branched held-out topology before any general adaptive-hierarchy claim. Full result: `docs/stage1-pilot-result-20260730.md`. |
| Revised Stage 1 P0 implementation | Added seven-class prime-modulus output heads, algebraic exact balancing after structure generation, five checkpointed curriculum stages, skew/balanced/branched generators, canonical shape IDs, content hashes, and separate node/depth/merge/compose telemetry. Target and rejection metadata remain outside token and structure-only inputs. |
| Privileged and sham controls | Reframed D as a privileged-structure diagnostic and added D-sham with identical architecture, copied initial weights, equal parameters, optimizer, learning rate, update count, examples, and compose count. D-sham deterministically rotates leaf/operator source alignments. |
| Revised evaluation and gate | Added named IID, depth-OOD, and held-out branched splits; fixed content seeds; per-class labels/predictions; cross-entropy; majority baseline; paired outcomes; seed mean/range; shape/content overlap audits; conservative D-true-over-A and D-true-over-D-sham conditions. Every single-run result keeps `stage2_unblocked=false`. |
| Confirmation execution path | Added validated `-TrainingSeed` selection from the frozen eight-seed list and `aggregate_stage1_confirmation.py`. Only an exact eight-seed set with the same source/config except seed, formal 10,000+ evaluation scale, and all candidate gates passing can produce the cross-run Stage 2 decision. |
| Revised focused tests | Final CPU suite passed 35 tests in `2.568s` with 4 expected DirectML skips. Final DirectML suite passed all 35 tests in `2.515s`. Coverage includes exact balance, independent label recomputation, deterministic generation, fail-closed rejection, topology/shape disjointness, forbidden structure fields, D-sham alignment, curriculum resume, 1029-example accounting, gate pass/fail, eight-seed aggregation, locks, checkpointing, and device-resident optimizer state. |
| Revised frozen smoke runs | Final-source CPU and DirectML five-step smoke runs completed every curriculum stage with zero warnings, content-disjoint and shape-valid audits, nonconstant learning gates, failed candidate gates, and Stage 2 blocked. DirectML fallback observability remained `unknown`. |
| Revised short learning gate | The final 50-step DirectML run completed 700 examples per model. A/D-true/D-sham training accuracy was `15.57%` / `13.86%` / `14.14%` versus balanced majority `14.29%`. Predictions used 3-7 legal classes, so the nonconstant development gate passed; candidate gate failed and no recursive advantage is claimed. Per-stage labels were exactly balanced. |
| Depth-1 structural bias repair | Initial learning telemetry showed forced repeated depth-1 leaves rejected roughly half of structures because subtraction became identically zero. Repetition is now forced only with at least three leaves. The final depth-1 acceptance rate was `97.22%`, and a focused test verifies accepted depth-1 data contains both add and subtract. |
| Revised preservation check | `docs/research-protocol.md` and the frozen pilot copy still match SHA256 `6FB366BC41F0A161B63FC2C10AB990CC47FF30509B0E7D3C37D7F333289E0A80`. Prior run artifacts and snapshots were not modified or deleted. No long formal run was launched. |

## Environment audit

- Operating system: Windows 10 Pro for Workstations `10.0.19045`.
- CPU/RAM: Ryzen 5 5600X / 32GB RAM.
- GPU: AMD Radeon RX 9060 XT; installed driver `32.0.21030.31`.
- VRAM authority: registry `HardwareInformation.qwMemorySize = 8,539,602,944` bytes, or approximately `7.95 GiB`; this project records it as **8GB**.
- Rejected value: the legacy 32-bit `MemorySize` result around 4GB is a truncation and is not a valid VRAM reading.
- Blocked: AMD's currently documented Windows PyTorch/ROCm environment route is for Windows 11 with Python 3.12, but AMD's current Radeon Windows limitations also state that ML training is unsupported. This Windows 10 machine must not receive a forced ROCm installation, OS upgrade, or driver upgrade for this project. Future GPU training should follow an then-current official Linux route, after rechecking its matrix.

## Pending verification

- The development learning gate establishes nonconstant legal outputs only.
  Its accuracy is near chance and does not establish D-true advantage.
- The eight-training-seed, 10,010-example-per-split/content-seed confirmation
  matrix completed and passed. It confirms fixed privileged-structure
  advantage, not learned hierarchy.
- Stage 2 learned `MERGE/STOP` remains unstarted even though the aggregate now
  authorizes beginning it.
- D-sham is one deterministic source-misalignment intervention; future
  confirmation may still need additional sham families if it proves too weak
  or topology-dependent.
- DirectML fallback observability remains unknown.
| Literal arithmetic revision | Added explicit `literal` versus `bound_variable` operand modes. Literal leaves use tokens `8..14`; independent leaf coefficients are computed after topology/operator generation and one nonzero leaf is solved modulo seven after balanced target selection. Bound-variable generation remains available but is structurally gate-ineligible. |
| Literal foundation contract | Added fixed five-stage literal curriculum validation with C0/C1 rehearsal, first-class stage-boundary evaluations, checkpoint/resume persistence, and fixed C0 `0.99` / C1 `0.98` A and D-true thresholds on at least 700 balanced examples. D-sham remains compute-matched but is not required by the foundation gate. |
| Literal execution tiers | Added separate candidate and formal DirectML configs. Candidate uses effective batch 56, divisible by both 7 and 8. Formal remains unstarted and preserves eight training seeds plus 10,010 examples per split/content seed. |
| Literal DirectML candidate result | `runs/stage1-20260730T131533Z` completed 500 paired steps / 28,000 examples per model in `232.281s` with no runtime warnings. On the final fixed balanced 700-example sets, C0 A/D-true were `1.0000/1.0000`; C1 A/D-true were `0.567143/1.0000`. Each class occurred exactly 100 times. The learning/non-collapse check passed, but the immutable foundation gate and structural candidate gate failed. No formal run was launched. |
| Active evaluation exclusion repair | Replaced post-hoc-only content auditing for final evaluation with deterministic active rejection against training hashes and one shared cross-split/seed accepted set. Added separate exclusion/quota counts and a configurable hard attempt bound that fails closed without partial data. |
| Longer literal structural candidate plan | Added a new, unstarted 8,000-step DirectML candidate using A32/D32 and learning rate `1e-3`: C0 200; C1 1,600 with 1:3 rehearsal; depth-2 1,800; depth-3 1,800; final rehearsal 2,600. Candidate final evaluation remains 700 examples x 2 seeds. The formal literal config was deliberately not updated because no result exists for this candidate. |
| Bounded overlap-repair validation | CPU focused exclusion/config tests passed 4/4 in `1.351s`; the DirectML Stage 1 suite passed 28/28 in `1.900s`. Compileall, both pip checks, the 63-file text check, and the unchanged research-protocol SHA256 passed. Neither the 8,000-step candidate nor any formal run was launched. |
| Formal completion integrity repair | Reserved `completed` for exact target-step completion. STOP/timeout now produce fail-closed `incomplete` results; runtime exceptions produce `failed` results. Result schema 3 records global/target steps, aggregation eligibility, config digest, and shared completion checks. |
| Candidate evidence binding | Formal candidate authorization now requires pinned canonical candidate-config, source-manifest, snapshot-manifest, and result-file SHA256 values plus exact operand mode, completion, evaluation scale/seeds, and passing foundation/candidate/learning gates. Empty formal pins deliberately block launch. |
| Pre-final exclusion closure | Heartbeat and stage-boundary/foundation accepted hashes are checkpointed and supplied as a separate exclusion domain to final evaluation. Final overlap audit now verifies zero overlap with training, pre-final evaluations, and all prior final split/seed data. |
| Formal paired statistics | Formal seed results now emit per-sample A/D-true/D-sham correctness masks. The eight-seed aggregator recomputes paired effects, requires exact complete-run/spec/source evidence, and applies one-sided paired Student-t lower bounds with Bonferroni familywise alpha `0.05` over all split/effect comparisons. Missing masks fail closed. |
| D-sham v1 repair | Replaced the fixed source rotation with preregistered `content-keyed-derangement-v1`, using domain-separated SHA256 mappings while preserving parameters and compose calls. Documented C0 as nondiscriminative and C1 as a limited two-leaf control. |
| Formal seed reservation | Literal formal evaluation now reserves unused seeds `51047`, `61051`, and `71059`; the older formal config uses a separate unused set. No formal process was launched, and the active `runs/stage1-20260730T135229Z` process/snapshot was not controlled or modified. |
| Formal-blocker terminal validation | CPU passed 46 active tests with 5 expected DirectML skips (`51` discovered) in `5.236s`; DirectML passed `51/51` in `5.650s`. Both pip checks, compileall, PowerShell parsing, the 63-file text check, and unchanged research-protocol SHA256 passed. No candidate or formal run was launched. |
| 8,000-step candidate audit | Read-only inspection confirmed `runs/stage1-20260730T135229Z` completed `8000/8000`, passed foundation/content/shape checks, and had no warnings. D-true reached `1.0` on all four splits and all D advantage conditions passed. The run remains `failed-under-original-gate` because A missed majority plus `0.03` on ID-balanced and both OOD splits. |
| Completion-schema compatibility | Confirmed the immutable result is schema 2: observed step is `checkpoint_recovery.current_step`, target is `config.optimizer_steps`, and top-level schema-3 aggregation fields are absent. The checker now locates historical completion without inferring formal eligibility; candidate authorization and aggregation require schema 3 plus explicit eligibility. |
| Post-hoc baseline amendment | Added `privileged_structure_posthoc_v1` as an explicit candidate-only policy. It keeps D thresholds, requires D-true above baseline on all required splits, A nonconstant everywhere, and A above the unchanged floor on at least one IID split. The old result is not reclassified. |
| Fresh revalidation plan | Added an unlaunched 8,000-step config with new training seed `82421`, final evaluation seeds `92041`/`92051`, and foundation seed `92063`. The formal literal config remains unchanged, unpinned, and blocked pending an independent candidate pass. |
| Post-hoc repair validation | CPU discovered 53 tests: 48 passed with 5 expected DirectML skips in `4.551s`; DirectML passed 53/53 in `5.035s`. Compileall, both pip checks, the 66-file text check, PowerShell parsing, and the unchanged research-protocol SHA256 passed. No training run was launched. |
| Historical final-hash durability | Added checkpoint-schema-3 persistence for cumulative historical final-evaluation content hashes. Resume restores and separately excludes/audits them. Legacy checkpoints containing final data without recoverable hashes now fail closed. A STOP-final-checkpoint-resume-final regression test verifies zero reuse. |
| Scientific-spec candidate binding | Added canonical `validated_experiment_spec_digest`, using default inclusion and an explicit runtime-only exclusion allowlist. Candidate result, formal pin, worker, and aggregator must agree. The current formal config retains a blank pin and incompatible old curriculum, so identity pins cannot authorize it. |
| Strict schema booleans | Replaced schema truthiness coercion with literal boolean validation in config, completion checks, candidate authorization, overlap checks, and aggregation. Tests prove `"false"` and `"true"` fail closed. |
| Second-review terminal validation | CPU discovered 56 tests: 51 passed with 5 expected DirectML skips in `5.959s`; DirectML passed 56/56 in `6.387s`. Compileall, both pip checks, the 66-file text check, PowerShell parsing, unchanged research-protocol SHA256, and unchanged 8,000-step result SHA256 all passed. No training was launched. |
| Fresh post-hoc candidate result | Read-only verification confirmed `runs/stage1-20260730T152137Z/result.json` SHA256 `6F964AE37B7354FB0F8E3B228F4BF3BF0B34C90CE6BC820F312BECAD8B49D615`: schema 3, completed `8000/8000`, aggregation eligible, foundation/content/shape/learning/candidate gates passed, and zero warnings. This remains one candidate seed, not formal confirmation. |
| Formal compatibility binding | Preserved the candidate's immutable original experiment-spec digest and added a canonical compatibility digest that permits only the required fresh confirmation training-seed set and foundation seed in addition to already allowed final-evaluation scale/seeds and runtime controls. Curriculum, optimizer, models, learning rate, data, topology, gates, foundation scale/thresholds, and statistics remain bound. Worker and aggregator independently revalidate both roles. |
| Prepared formal confirmation | Added an unlaunched 8,000-step DirectML formal config pinned to the passing candidate. It reserves eight fresh training seeds `931101` through `931211`, formal evaluation seeds `941101`/`941119`/`941129`, foundation seed `941149`, and 10,010 examples per split/seed. The older formal config remains blocked. |
| Sequential formal runner | Added a resumable, fail-closed serial runner. It validates candidate evidence before sequence creation, starts one DirectML worker at a time, requires a valid terminal formal result before advancing, and invokes aggregation only after all eight results verify. No training was launched during implementation. |
| Formal-preparation validation | CPU discovered 62 tests: 57 passed with 5 expected DirectML skips in `11.817s`; DirectML passed `62/62` in `12.360s`. Focused Stage 1 suites passed `42/42` in both environments. Direct CPU/DirectML CLI entrypoint checks, coordinator-lock and serial-stop tests, compileall, both pip checks, PowerShell parsing, the 69-file text check, candidate preflight `26/26`, seed-freshness tests, and unchanged protocol/old-run/candidate SHA256 checks passed. No formal run directory or process was created. |
| Formal holdout isolation repair | Formal final generation now requires exact target completion. STOP, per-session timeout, and training failures save checkpoint/status/result only. An atomic final-attempt marker prevents repeated holdout access and permits reconstruction only from recoverable completed final state. |
| Sequence recovery repair | Recoverable incomplete results are preserved under `attempt-results` and resumed from checkpoint; user STOP waits for explicit Resume, timeout receives a fresh worker-session budget, and worker exceptions are classified nonrecoverable. The monitor waits for actual worker exit. Pid-less shells are rebuilt only when strict zero-evidence checks pass. |
| Formal provenance and seed gate | Added file-backed source/snapshot verification shared by candidate preflight, formal result checking, and aggregation. Every listed file and both manifest hashes are recomputed; missing/malformed evidence fails closed. Runtime freshness scanning rejects any formal seed reused by historical nonformal config/run evidence and enforces pairwise disjoint training/eval/foundation seeds. |
| Final-release repair validation | CPU discovered 70 tests: 65 passed with 5 expected DirectML skips in `19.432s`; DirectML passed `70/70` in `19.816s`. Focused Stage 1 suites passed `52/52` on CPU in `20.006s` and DirectML in `19.208s`. Compileall, both pip checks, PowerShell parsing, the 70-file text check, candidate preflight `35/35`, runtime seed freshness, and immutable protocol/old-run/candidate SHA256 checks passed. No formal run was launched. |
| Resume-time floating-boundary repair | Added an explicit `1e-9s` absolute tolerance to the cumulative elapsed-time test assertion, covering platform floating-point cancellation without changing the per-session timeout comparison. The focused test passed `1/1` on CPU in `1.173s` and DirectML in `0.657s`; full CPU passed 65 tests with 5 expected skips in `13.226s`, and DirectML passed `70/70` in `13.970s`. No training was launched. |
| Formal seed 1 evidence | Read-only verification confirmed `runs/stage1-formal-01-931101-20260730T172025Z/result.json` is schema 3, completed `8000/8000`, eligible, candidate-passing, and has a completed one-time formal-final marker. Its immutable result SHA256 is `AD2CE19CA0136DE60AC51F0CCC2F5D666E56F79DACE19B2D9B68D9E4F1B541D1`. |
| Formal seed 2 legacy-stop repair | `runs/stage1-formal-02-931117-20260730T193056Z` stopped at `7255/8000` with an emergency checkpoint and untouched formal holdout, but the old worker called the final-dependent learning gate and wrote a failed result. Incomplete formal finalization no longer calls that gate. The sequence accepts only this exact historical error after 30 strict schema/config/digest/candidate/manifest/checkpoint/holdout/status checks, waits for explicit STOP removal, and archives the original result bytes unchanged before resume. No training was launched during repair. |
| Formal provenance mismatch discovered | Seed 1 and seed 2 already have different source/snapshot manifests; the only snapshot file differences are the sequential coordinator and its test file. The existing aggregator deliberately requires identical source and snapshot manifests, so final aggregation remains fail-closed unless this provenance incompatibility receives a separate scientific resolution. This repair does not relax that requirement. |
| Legacy-stop repair validation | The soft-Stop and strict legacy-migration tests passed in both CPU and DirectML environments. Full CPU discovered 72 tests: 67 passed with 5 expected DirectML skips in `16.849s`; DirectML passed `72/72` in `17.503s`. The real seed-2 artifact passed all `30/30` recovery checks without modification. |
| Legacy formal queue classification | The original sequence state, seed-1 result, seed-2 result/checkpoint/STOP, and their run snapshots remain byte-preserved. Because seed 1 and seed 2 have different complete source/snapshot manifests, the old queue is abandoned for formal statistics without weakening the aggregator's same-manifest rule. Seed 1 remains an observed positive old-queue result; seed 2 remains untouched-holdout engineering evidence. |
| Canonical formal campaign v2 | Added a versioned formal config with twelve mutually disjoint historically unused seeds, one pre-run canonical snapshot package, generated environment/candidate identity receipts, atomic campaign state, prepared-run materialization, and result-to-campaign source/snapshot pin verification. The v2 config/state/output/run prefix and coordinator lock are isolated from the legacy queue. |
| Campaign-v2 implementation validation | Focused canonical tests passed `5/5` on CPU in `1.671s` and DirectML in `1.665s`. Full CPU discovered 77 tests: 72 passed with 5 expected DirectML skips in `17.889s`; DirectML passed `77/77` in `18.347s`. Candidate preflight passed `35/35`; seed freshness found zero overlap; compileall, both pip checks, all PowerShell parses, the 71-file UTF-8/NUL check, and six immutable protocol/legacy-evidence hashes passed. No campaign-v2 training run was launched. |
| Campaign-v2 independent launch rejection | A final read-only review found that v2 launch receipts could be re-read as historical seeds after coordinator restart and that launch/resume selected `start_stage1.ps1` from the mutable worktree. V2 was rejected before any training launch; its campaign package and ready state remain immutable evidence. |
| Canonical formal campaign v3 repair | Added a new fresh twelve-seed registry and versioned v3 config/state/output/run namespace. V3 explicitly excludes its state, aggregate, receipts, campaign root, and state-owned runs from its own freshness scan while retaining every other historical record. Execution is accepted only from the campaign's canonical coordinator; launch and resume use each materialized run's canonical PowerShell launcher with an explicit DirectML Python. The environment identity now binds and rechecks the Python executable/digest, dependency locks and versions, computer, GPU PNP identity, and driver before every seed. |
| Campaign-v3 pre-freeze validation | CPU discovered 81 tests: 76 passed with 5 expected DirectML skips in `21.894s`; DirectML passed `81/81` in `22.576s`. Both pip checks, compileall, all PowerShell parses, and the 77-file text check passed. The twelve v3 seeds were pairwise distinct with zero overlap across 60 historical seed values and no unreadable evidence. Protocol, legacy results/checkpoint, and the rejected v2 package hash remained unchanged. No v3 run or worker was created. |
| Campaign-v3 independent import rejection | Frozen v3 repeated preparation and both post-freeze test suites passed, but a second independent review found one remaining mutable dependency. Runtime `__file__` evidence confirmed the canonical coordinator loaded `scripts` from its snapshot while `dynamic_hierarchy.stage1_campaign` came from the editable worktree `src`. V3 was rejected before any run or worker launch and remains immutable evidence. |
| Canonical formal campaign v4 import repair | Added a fourth fresh twelve-seed registry and v4 namespace. The coordinator now places both `canonical-snapshot/src` and the snapshot root before the editable install, then fails immediately if any loaded `dynamic_hierarchy` or `scripts.*` module resolves outside the expected frozen roots. Clean-subprocess tests prove canonical imports resolve inside a copied snapshot, while deliberately preloading a worktree module is rejected. |
| Campaign-v4 pre-freeze validation | CPU discovered 83 tests: 78 passed with 5 expected DirectML skips in `27.022s`; DirectML passed `83/83` in `27.377s`. Both pip checks, compileall, all PowerShell parses, and the 80-file text check passed. The twelve v4 seeds were pairwise distinct with zero overlap across 72 historical seed values and no unreadable evidence. Protocol, legacy result/checkpoint, and both rejected campaign-package hashes remained unchanged. No v4 run or worker was created. |

## 2026-07-31

| Command or action | Result |
| --- | --- |
| Canonical campaign-v4 execution | The frozen coordinator ran exactly the declared seeds `991501`, `991511`, `991531`, `991541`, `991547`, `991567`, `991579`, and `991589` serially. All eight runs reached `8000/8000`, completed formal final evaluation, passed foundation and structural gates, emitted zero runtime warnings, and were marked `verified`. Campaign wall time from first worker start to final status was `30,216.089s`. |
| Formal aggregate decision | `runs/stage1-literal-formal-v4-confirmation.json` records `formal_confirmation_passed` and `stage2_unblocked=true`. All `304/304` per-run integrity checks, six top-level conditions, and eight Bonferroni-corrected statistical conditions passed. Aggregate SHA256 is `95F4147F05CD31C1133418AF17A2F3061B37044A265D7FF0BB0BB06EBEE15631`. |
| Independent aggregate reproduction | Ran the frozen aggregate script over the exact eight verified result paths with both frozen Python source roots ahead of the editable install. The 22,131-byte recomputation was byte-identical and had the same SHA256 as the canonical aggregate. |
| Scientific decision boundary | Stage 1 formally confirms that supplied true structure greatly outperforms both A and the architecture-matched wrong-structure sham in this synthetic modulo-seven experiment. It authorizes starting Stage 2 but does not claim learned hierarchy, learned boundaries, or working `MERGE/STOP`. Full report: `docs/stage1-formal-v4-confirmation-result-20260731.md`. |
| Post-campaign freshness-test repair | The first post-run CPU suite exposed two state-dependent tests: they asserted that retired v2/v3 seeds remained globally fresh after completed v4 run snapshots had legitimately recorded those old configs. Production freshness logic was unchanged. The unit cases now use isolated temporary history, and a new completed-project test proves every v2/v3 registered seed is historical and both retired campaigns fail closed. The three focused tests passed. |
| Terminal dual-environment validation | CPU discovered 84 tests: 79 passed with 5 expected DirectML skips in `28.340s`; DirectML passed `84/84` in `30.373s`. Compileall, both pip checks, both PowerShell parses, and the 81-file no-trailing-whitespace check passed. Protocol, legacy results/checkpoint, candidate result, v2/v3/v4 campaign manifests, and the v4 aggregate matched all nine expected SHA256 values. Three DirectML test child processes observed during teardown exited within three seconds; the final project-process count was zero. |
| Public plain-language research overview | Added `docs/research-overview-zh.md` and linked it near the top of `README.md`. The guide explains the original intuition, controlled Stage 1 comparison, confirmed facts, interpretation limits, and Stage 2 direction without changing code, configurations, or experiment evidence. |
| README homepage consolidation | Merged the complete Chinese plain-language overview into the top of `README.md`, corrected its repository-root links, and removed the now-duplicate standalone guide. The previous standalone version remains recoverable from Git history. |

## 2026-08-09

| Command or action | Result |
| --- | --- |
| Stage 2 preconstruction audit | Rechecked the clean `main` worktree, Stage 1 result boundary, current data/model APIs, and nearest dynamic-structure/adaptive-compute work. Confirmed that the existing candidate controller neither consumes a query nor executes a merge, so Stage 2 remained unimplemented before this work. |
| Straight-through backend spike | A deterministic hard-forward/soft-backward adjacent-choice probe produced finite, nonzero router and state gradients on CPU and DirectML. The pinned DirectML runtime rejected `one_hot`/scatter for the probe shape (`DirectML scatter doesn't allow partially modified dimensions`); an equality-and-broadcast construction produced matching CPU/DirectML results. This is an operator feasibility check, not model-training evidence. DirectML fallback observability remains unknown. |
| Stage 2 R1 independent rejection | An independent read-only audit rejected the initial multi-expression proposal before implementation. A query-aware fixed tree or readout could explain that task; base-family hashing, hard-forward isolation, executable fixed/sham/compute controls, full compute accounting, and preregistered effect margins also needed stronger treatment. |
| Stage 2 construction packet R2 | Replaced the uncommitted R1 draft with `docs/stage2-construction-packet-r2.md`. R2 uses one unparenthesized expression with two operator-precedence queries that induce different oracle trees and labels. It freezes post-generation family balancing, family-level isolation, hard-path answer access, query-blind/sham/fixed/compute controls, complete budget accounting, and one bounded `calibration_only` run. Positive candidate and formal claims remain not ready. No Stage 2 training was launched at this point. |
| R2 label-pair reachability audit | Exhaustively enumerated modulo-seven literals for every mixed ADD/SUB pattern with 3 through 6 leaves. Patterns containing `SUB` before a later `ADD` reached all 42 unequal ordered query-label pairs; monotone `ADD+ SUB+` patterns reached zero because precedence cannot change their value. R2 now rejects those impossible strata before generation rather than waiting for the quota ceiling. |
| R2 data and model construction | Added typed Stage 2 config/profile contracts, exact paired-family generation, query/label-free base hashes, duplicate rejection inside blocks, source-only oracle trees, hard adjacent MERGE/STOP execution, query-blind and query-permuted routers, all fixed policies, and a query-aware shared-recurrence hard-halting control. The answer path receives only hard-selected active states; a substitution test proves unselected candidate states cannot change the forward value. |
| R2 runtime and control matrix | Added one paired runtime for A-Q-param, A-Q-flop, A-recur, B-query, B-noQ-router, B-sham, F-stop/left/right/add/sub, D-true, and D-sham. It records common compute categories, synchronized wall time, parameter/optimizer receipts, DirectML peak-memory lower bounds, family canaries, traces, atomic status, a Windows run mutex, cooperative STOP, hysteretic resource pausing, and at-least-once checkpoints. |
| Focused and terminal validation | Final Stage 2 CPU focused tests passed `19/19`; explicit DirectML hard-router/backward and checkpoint-device tests passed `2/2`. The latest DirectML one-step smoke completed on `privateuseone:0` / AMD Radeon RX 9060 XT with zero train/eval family overlap and nonzero B router gradient/update. Final full CPU discovery passed `105` tests with `7` expected DirectML skips in `34.120s`. DirectML exposes no public fallback counter, so fallback remains unknown despite zero Python warnings. |
| Frozen R2 calibration | `runs/stage2-r2-calibration-directml-821101` completed `120/120` updates for all eight trainable controls in `801.203s`, using four CPU threads and one DirectML worker. Training contained 5,040 base-family draws / 10,080 query rows, 4,393 unique families, and 647 repeated training families. Final evaluation used 1,260 unique base families / 2,520 query rows with zero train/evaluation overlap. Parameter mismatch was `0.1144%`; full-candidate operation-estimate mismatch was `0.0504%`. No Python warnings were emitted. |
| Calibration finding | All models remained near the seven-class chance level. B-query selected immediate STOP on every row of all three evaluation profiles, producing zero tree edges and exactly matching F-stop behavior. D-true was not consistently above A or D-sham, so the calibration did not establish task learnability or a structural effect. The registered disposition remains `calibration_inconclusive`; no candidate or phase claim is authorized. |
| Structure telemetry repair | Post-run audit found that the trace writer emitted `MERGE` while `_trace_edges` filtered lowercase `merge`, invalidating only the original structure metrics. Added a regression requiring F-add/F-sub exact-tree rate `0.5`. The first `--reevaluate-only` attempt failed before checkpoint load because frozen JSON lists were compared to in-memory tuples; JSON-canonical comparison fixed resume and its focused test passed. Evaluation-only recovery reused the unchanged final checkpoint, performed zero optimizer updates, and produced task accuracy/cross-entropy byte-for-byte equal to the original values while correcting structure telemetry. |
| Calibration evidence retention | Preserved the original invalid-structure-telemetry result (SHA256 `2F5A6D315835A85F3AF32D4E0E8295EBCACB880CBF94DB04FDC5DDC6D16E96ED`) and the corrected evaluation-only result (SHA256 `03B9A949B2F8486B6D9F7DC40EFAFC7FFAEC22BE5E96AE8C0B13523D66911E6E`). The unchanged local final checkpoint SHA256 is `B65E0727E4A094D45A15B005F1232C1F1A0D97F69E78A6C227C78B22D0AF321E`; checkpoints remain local recovery artifacts. |
| R3 direction | Do not spend additional seeds or add continuous phase yet. First preregister a learning-feasibility gate that requires an ordinary or privileged-structure control to learn above chance. Then remove STOP from B on the full-expression precedence task and isolate query-conditioned MERGE order; variable useful stopping requires a separate task. |
| R3 feasibility construction slice | Added dual-revision Stage 2 config serialization so `stage2-r2` `to_dict()` stays JSON-field compatible with older frozen configs and checkpoints while `stage2-r3` gains explicit `phase`/gate fields. Replaced `stage2_model.py` and `stage2_runtime.py` with R2/R3-aware implementations: R3 learned B has no `stop_router`, `B-oracle` can run `forced_selected_only`, fixed interventions keep explicit `forced_candidate_matched` compute, R3 feasibility uses deterministic fixed n=3/n=4 training pools with checkpoint reconstruction evidence, routing train attempts fail closed as `not ReadyForRouting`, and feasibility results emit preregistered gate receipts/dispositions. Added `configs/stage2-r3-smoke-cpu.json`, `configs/stage2-r3-smoke-directml.json`, `configs/stage2-r3-feasibility-directml.json`, plus focused CPU/DirectML R3 tests. |
| R3 feasibility validation | CPU Stage 2 focused tests passed `26/26` in `6.493s`. DirectML Stage 2 focused tests passed `4/4` in `17.444s` after setting `PYTHONPATH=tests` for `test_directml.py`'s historical `test_stage1` import style. `python -m scripts.run_stage2 --config configs/stage2-r3-smoke-cpu.json` and the DirectML counterpart both completed from temporary output roots with disposition `feasibility_failed`, zero runtime warnings, exact oracle trace metrics (`edge_f1=1.0`, `exact_tree_rate=1.0`), `B-oracle.selection_path="forced_selected_only"`, and DirectML fallback observability still `unknown`. Full CPU and DirectML project discovery each failed on the same pre-existing Stage 1 launcher test: `Start-Process` rejected duplicate environment keys `Path` and `PATH` inside the snapshot `start_stage1.ps1`; this blocker is outside the Stage 2 write scope. `compileall`, both `pip check` commands, `scripts/check_project_text.py`, and `git diff --check` completed successfully, with only LF→CRLF Git warnings and no whitespace errors. |
| R3 PM repair and fresh terminal validation | Independent review restored R2 STOP scoring/candidate-matched F-stop behavior, required `StructureOnlyBatch` for oracle routing, froze the R3 gate constants, added fixed-pool exposure/balance evidence, hardened malformed gate handling, and corrected selected-only operation accounting. CPU Stage 2 focused tests then passed `27/27`. Fresh full CPU and DirectML discovery each passed `115/115` with the expected backend skips (`32.783s` and `49.881s` respectively). This supersedes the preceding transient `Path`/`PATH` failure, which came from the delegated process environment rather than repository code. R2 CPU smoke and R3 CPU/DirectML smoke all completed with their expected dispositions. |
| Frozen R3 feasibility calibration | `runs/stage2-r3-feasibility-directml-821301` completed the only authorized `600/600`-update run in `662.703s` on `privateuseone:0` / AMD Radeon RX 9060 XT. It used 84 unique fixed training families over 25,200 exposures and 84 disjoint evaluation families. Training cumulative accuracy reached 92.79% for A, 98.02% for B-oracle, and 98.15% for D-true, while held-out accuracy remained 2.38%-14.29%. B-oracle executed the oracle tree exactly with full reduction and no STOP, so all four preregistered B-oracle/D-true feasibility gates failed on answer accuracy and cross-entropy rather than structural execution. Disposition is `feasibility_failed`; routing, extra seeds, and Phase C were not started. Zero Python warnings were observed, while DirectML fallback remains `unknown`. |
| R3 public evidence | Preserved the exact frozen config and result under `evidence/stage2-r3-feasibility/`. Result SHA256 is `A57C93DAAAA1E76FAD2E3F989A1BE43D6EE8BA18AE4FAEDDCEB4F2E1612B697B`; frozen-config SHA256 is `AD6323C4CEE1C39EEE63EC5AC1DF6D30EF04480C3F196463ADBD0ADE4F56885E`. The local final checkpoint remains an unpublished recovery artifact with SHA256 `8F625813562743D2E4577E2925C49BE0D873E0F5D86A973F6E36402CF0A7416A`. |
| R4 construction packet | Froze `DH-S2-R4` as a data-diversity feasibility slice: 1,680 train, 336 validation, and 336 reserve families exhaust the legal n=3/n=4 domains. Forty balanced train blocks preserve R3's 25,200 total family exposures while increasing unique train coverage 20-fold. Validation failure must leave reserve ungenerated and unopened. |
| R4 construction and independent audit | Added backward-compatible R4 config serialization, deterministic disjoint block scheduling, exact exposure/reconstruction receipts, validation-first evaluation, a persistent one-shot evaluation ledger, and fail-closed causal B-oracle gate checks. An initial independent audit blocked calibration on reserve replay safety, R3 checkpoint compatibility, incomplete config freezing, and missing receipts. Those findings were repaired; re-audit returned `ReadyForCalibration`, including a successful load of the official R3 step-600 checkpoint. |
| R4 validation | CPU Stage 2 focused tests passed `35/35`; DirectML Stage 2 focused tests passed `5/5`. CPU and DirectML one-step R4 smokes completed with expected `feasibility_failed` dispositions, exact B-oracle selected-only structure receipts, zero overlap, and unopened reserves. Reopening the same completed CPU smoke reused the byte-identical ledger without reevaluation. R2/R3 CPU regression smokes completed with expected dispositions. Full CPU discovery passed `124` tests with `10` expected skips in `43.472s`; full DirectML discovery passed `124/124` in `67.309s`. `compileall`, both `pip check` commands, project text validation, and `git diff --check` passed. |
| Frozen R4 feasibility calibration | `runs/stage2-r4-feasibility-directml-821401` completed the only authorized `600/600`-update run in `801.5s` on `privateuseone:0` / AMD Radeon RX 9060 XT. All 1,680 train families received exactly 15 exposures. Training accuracy remained near chance for A-Q-param (15.21%), B-oracle (15.21%), and D-true (15.15%). On 336 disjoint validation families, all four required B-oracle/D-true accuracy and cross-entropy gates failed. B-oracle still had exact-tree, edge-F1, and full-reduction rates of 1.0 with no STOP, so the disposition is `feasibility_failed` rather than an implementation failure. |
| R4 reserve and public evidence | The persistent ledger recorded `validation_failed`; `reserve_opened=false`, and no reserve family was generated or evaluated. Published the exact result, frozen config, and ledger under `evidence/stage2-r4-feasibility/`. Their SHA256 values are `561A35F62608A23751E42249586A2B1EA8503AFC651E46A959AEDA851BD2869D`, `92788FFD78635D90C142135AE10DFE9735AA17C85C5AEEE714AC67A4A240A693`, and `C887BD2C376345E12426FBDAC813AD1D116C98DB8788C416AD1A0B61A5C5425D`. The local checkpoint remains unpublished with SHA256 `D3DAD19620C594CED11E760275D03B2BCD85917197FC7F1DD7ED8D7B5023AC60`. |
| Post-R4 direction | Do not start learned routing, more seeds, or continuous phase. The next packet should isolate single-step modulo-seven representation, fixed-query composition, paired-query oracle composition, and diagnostic intermediate supervision before routing resumes. |
| R5.1 construction and audit | Froze `DH-S2-R5.1` as an arithmetic causal ladder with one exhaustive 98-row binary rung and one common 210/42/42 split for fixed and paired n=3 queries. Independent audits blocked calibration on partial-branch and binary gate recovery, unstable/global sham matching, and a paired answer/structure admission bug. Added exact ledger snapshots with monotonic evidence recovery, stable per-query sham derangement, separate paired answer/structure aggregation, and regression tests for every reported crash or attribution path. |
| R5.1 terminal validation | Focused CPU R5 tests passed `20/20`; focused DirectML R5 auxiliary and checkpoint paths passed. CPU and DirectML smokes traversed all three rungs with zero Python warnings. Final full CPU discovery passed `146/146` with 12 expected DirectML skips; DirectML passed `146/146`. Compileall, both pip checks, project text validation, `git diff --check`, and live loading of retained R3/R4 step-600 DirectML checkpoints passed. Two independent re-audits returned `ReadyForCalibration`. |
| Frozen R5.1 arithmetic ladder | `runs/stage2-r5-ladder-directml-821501` completed the only authorized run in `107.672s`, ending at global round 600 with `fixed_query_failed`. Binary root passed 98/98 with cross-entropy 0.0000263. Fixed ADD root scored 41/42 and fixed SUB root 40/42, so both root reserves and all paired models remained closed. Both teacher-state branches scored 42/42 on validation and reserve; both aux-true branches decoded the intermediate value perfectly but still scored 41/42 on final answers. Zero Python warnings were observed; DirectML fallback remains `unknown`. |
| R5.1 evidence and decision | Published the exact config, result, and one-shot ledger under `evidence/stage2-r5-arithmetic-ladder/` with SHA256 values `4B64023623B3DE1AC23D06E718ADA1C9BB639085CF95688A4EAC1FED03D5DCA7`, `3A56C05A3A8566A3E1E0AFEE1628329B7DCC83DD0C37EC336D67053F103C3B1B`, and `8847E7BD0F1098A930B9C8DD725D2AA0DCBD197B91E1FED90286132AE7961ACD`. The final checkpoint remains local with SHA256 `18327E373F937D353297811DB60C7180B9B3823FE49B4E7CDB09EE27D6EFD489`. Do not add seeds or start routing; next isolate operational merge-state alignment with a matched sham. |
| R5 state diagnostic construction | Froze `DH-S2-R5D-R1` before implementation, then added a CPU-only checkpoint diagnostic and runner. It binds the exact R5 checkpoint, partition, config, result, and ledger hashes; reconstructs validation only; forbids reserve materialization, backward calls, optimizer updates, paired models, and output under the canonical run; and records replay, source-manifest, intervention, transition, and pre/post artifact receipts. Five focused tests cover exact replay, bijective transplants, semantic counterfactual targets, no-training/no-reserve evidence, and fail-closed artifact mutation. |
| R5 state diagnostic result | The final read-only diagnostic completed all eight fixed-query branches with exact integer replay, zero backward/optimizer activity, unopened reserves, and unchanged canonical hashes. Wrong-value generated-state transplants changed all predictions and retained `41/42` to `42/42` counterfactual arithmetic accuracy, while correct literal-state injection hurt root accuracy. Same-value swaps repaired both SUB root errors but produced one repair and one damage for ADD root. Generated states are therefore operational, but same-value instances are not yet perfectly interchangeable. |
| R5 diagnostic evidence and R6 packet | Published the exact diagnostic result under `evidence/stage2-r5-state-diagnostic/`; result SHA256 is `2A81640994726E8FF5E75CB7FC01ADA59C3CC0DAC38A8E3B4B7B5DFD3D54DAF0`. Added `DH-S2-R6-R1`, which proposes matched true/sham state-congruence training on a new `+-` family domain. R6 remains `NotReadyForConstruction` pending independent audit and does not authorize code or training. |
| R5 diagnostic terminal validation | Focused diagnostic tests passed `5/5`. Final full CPU discovery passed `151` tests with 12 expected DirectML skips in `60.764s`; DirectML passed `151/151` in `82.286s`. `compileall`, both environment `pip check` commands, the 149-file text check, `git diff --check`, the evidence checksum, and all five recorded source-manifest hashes passed. Exhaustive enumeration separately confirmed the proposed R6 split has 343 unique joint states, exact 245/49/49 counts, and uniform final/ADD-intermediate/SUB-intermediate marginals in every split. |
